#########
Reference
#########
.. note:: For backwards compatibility all constants, functions and classes in 
   pifacecommon's sub-modules (core, interrupts, errors) have been imported into 
   the main namespace. :func:`pifacecommon.get_bit_num(0)` is the same as 
   :func:`pifacecommon.core.get_bit_num(0)`.

.. automodule:: pifacedigitalio
   :members:
